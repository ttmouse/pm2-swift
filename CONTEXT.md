# CONTEXT: pm2-swift

## 核心概念

- **PM2 项目**：由 PM2 管理的单个进程，通过进程名（`id`）唯一标识。每个项目有状态（online/stopped/errored 等）、端口、CPU/内存使用率、运行时长等属性。

- **服务分类**：按项目角色划分的类别——`api`、`frontend`、`bot`、`monitor`、`game`、`database`、`other`。根据项目名称推断（参见 pm2_wrapper.js 的 `categorizeApp` 函数）。

## 分组机制

- **分组**：将同一服务的前端/后端等相关项目自动归入同一组。分组基于项目 ID 的命名约定 `{project-name}-{role}`。例如 `xm-console-api` + `xm-console-frontend` 归入组 `xm-console`。

- **projectGroupKey**：决定项目归属哪个组的规则。取项目 ID 前两段连字符分隔部分作为组名。后备策略：不足两段时取唯一段 → 下划线分隔首段 → `"未分组"`。

- **组操作**：一键启动组内所有已停止的项目、一键停止组内所有运行中的项目。操作期间组开关锁定为"处理中"状态，操作完成后更新为真实状态。

## 错误处理

- **逐项目错误**：每个项目的启动/停止独立执行，操作结果（成功或失败）逐项目记录。

- **聚合错误**：组级操作完成后，收集所有子操作的真实结果。全部成功时通知"全部完成"，部分失败时列出失败项目及具体错误原因。不再通过事后轮询状态来"推断"失败。

## 排序

- **组排序**：按活跃状态排序——有在线项目的组排前面，同状态组按字母序排列。

- **组内项目排序**：继承全局排序规则（名称/状态/CPU/内存/运行时长）。

## 过滤

- 分组视图响应类别过滤和文本搜索。搜索结果中，只显示匹配项目所在的组和项目。
